import type { Config } from "tailwindcss";

const config: Config = {
    theme: {
        extend: {
            fontFamily: {
                sans: ["'Urbanist'", "sans-serif"],
            },
        },
    },
    plugins: [],
};

export default config;